import { EventEmitter } from 'ee-typed';
import Key from './Key';
type Events = {
    open(): void;
    close(): void;
    message(data: unknown): void;
    error(err: Error): void;
};
export default class RtcPairSocket extends EventEmitter<Events> {
    readonly pairingCode: string;
    readonly party: 'alice' | 'bob';
    private conn?;
    private closed;
    private cipher;
    private alicePeerId;
    private bobPeerId;
    private peerId;
    constructor(pairingCode: string, party: 'alice' | 'bob');
    private connect;
    send(data: unknown): void;
    close(): void;
    isClosed(): boolean;
    getKey(): Key;
}
export {};
//# sourceMappingURL=RtcPairSocket.d.ts.map