import { EventEmitter } from "ee-typed";
import { IO } from "./types";
export default class BufferedIO extends EventEmitter<{
    error(e: Error): void;
}> implements IO {
    send: IO['send'];
    private closeOther?;
    private bq;
    constructor(send: IO['send'], closeOther?: IO['close']);
    close(): void;
    recv(len: number): Promise<Uint8Array>;
    accept(data: Uint8Array): void;
}
//# sourceMappingURL=BufferedIO.d.ts.map