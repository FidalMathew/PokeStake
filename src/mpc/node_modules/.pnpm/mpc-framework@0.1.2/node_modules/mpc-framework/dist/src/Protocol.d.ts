import { Backend, Circuit, MpcSettings } from "mpc-framework-common";
import Session from "./Session";
export default class Protocol {
    circuit: Circuit;
    mpcSettings: MpcSettings;
    backend: Backend;
    constructor(circuit: Circuit, mpcSettings: MpcSettings, backend: Backend);
    join(name: string, input: Record<string, unknown>, send: (to: string, msg: Uint8Array) => void): Session;
}
//# sourceMappingURL=Protocol.d.ts.map