import { BackendSession, Circuit, MpcSettings } from "mpc-framework-common";
export default class PlaintextBackendClientSession implements BackendSession {
    circuit: Circuit;
    mpcSettings: MpcSettings;
    name: string;
    input: Record<string, unknown>;
    send: (to: string, msg: Uint8Array) => void;
    hostName: string;
    outputReceived: {
        promise: Promise<Record<string, unknown>>;
        resolve: (value: Record<string, unknown>) => void;
        reject: (reason?: any) => void;
    };
    inputsSent: boolean;
    constructor(circuit: Circuit, mpcSettings: MpcSettings, name: string, input: Record<string, unknown>, send: (to: string, msg: Uint8Array) => void, hostName: string);
    handleMessage(from: string, msg: Uint8Array): void;
    output(): Promise<Record<string, unknown>>;
}
//# sourceMappingURL=PlaintextBackendClientSession.d.ts.map