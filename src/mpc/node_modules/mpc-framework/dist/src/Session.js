export default class Session {
    circuit;
    mpcSettings;
    backend;
    name;
    input;
    send;
    backendSession;
    constructor(circuit, mpcSettings, backend, name, input, send) {
        this.circuit = circuit;
        this.mpcSettings = mpcSettings;
        this.backend = backend;
        this.name = name;
        this.input = input;
        this.send = send;
        this.backendSession = backend.run(circuit, mpcSettings, name, input, send);
    }
    handleMessage(from, msg) {
        this.backendSession.handleMessage(from, msg);
    }
    async output() {
        return await this.backendSession.output();
    }
}
//# sourceMappingURL=Session.js.map