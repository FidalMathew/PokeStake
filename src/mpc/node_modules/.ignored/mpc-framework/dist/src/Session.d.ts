import { Backend, BackendSession, Circuit, MpcSettings } from "mpc-framework-common";
export default class Session {
    circuit: Circuit;
    mpcSettings: MpcSettings;
    backend: Backend;
    name: string;
    input: Record<string, unknown>;
    send: (to: string, msg: Uint8Array) => void;
    backendSession: BackendSession;
    constructor(circuit: Circuit, mpcSettings: MpcSettings, backend: Backend, name: string, input: Record<string, unknown>, send: (to: string, msg: Uint8Array) => void);
    handleMessage(from: string, msg: Uint8Array): void;
    output(): Promise<Record<string, unknown>>;
}
//# sourceMappingURL=Session.d.ts.map