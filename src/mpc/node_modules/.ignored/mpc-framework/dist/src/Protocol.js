import Session from "./Session";
export default class Protocol {
    circuit;
    mpcSettings;
    backend;
    constructor(circuit, mpcSettings, backend) {
        this.circuit = circuit;
        this.mpcSettings = mpcSettings;
        this.backend = backend;
    }
    join(name, input, send) {
        return new Session(this.circuit, this.mpcSettings, this.backend, name, input, send);
    }
}
//# sourceMappingURL=Protocol.js.map