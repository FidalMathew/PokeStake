import PlaintextBackendHostSession from "./PlaintextBackendHostSession";
import PlaintextBackendClientSession from "./PlaintextBackendClientSession";
export default class PlaintextBackend {
    run(circuit, mpcSettings, name, input, send) {
        const hostName = mpcSettings[0].name ?? "0";
        if (name === hostName) {
            return new PlaintextBackendHostSession(circuit, mpcSettings, name, input, send);
        }
        return new PlaintextBackendClientSession(circuit, mpcSettings, name, input, send, hostName);
    }
}
//# sourceMappingURL=PlaintextBackend.js.map