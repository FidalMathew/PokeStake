import { Circuit } from "mpc-framework-common";
export default function evaluate<T>(circuit: Circuit, inputs: Record<string, unknown>, arithmetic: Arithmetic<T>): Record<string, T>;
type Arithmetic<T> = {
    init(source: unknown): T;
    combine(op: string, inputs: T[]): T[];
};
export declare const u32Arithmetic: {
    init(source: unknown): bigint;
    combine(op: string, [a, b]: bigint[]): bigint[];
};
export {};
//# sourceMappingURL=evaluate.d.ts.map