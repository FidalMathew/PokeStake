import { Circuit, MpcSettings } from "mpc-framework-common";
export default class EmpCircuit {
    private bristol;
    private info;
    private metadata;
    private gates;
    private aliceInputs;
    private bobInputs;
    private allInputs;
    private outputs;
    private wireIdMap;
    private firstOutputWireId;
    private nextWireId;
    private nextOutputWireId;
    private zeroWireId?;
    constructor(circuit: Circuit, mpcSettings: MpcSettings);
    private getWireId;
    private assignWireId;
    private getZeroWireId;
    private getInputWidth;
    private getOutputWidth;
    getSimplifiedBristol(): string;
    encodeInput(party: 'alice' | 'bob', input: Record<string, unknown>): Uint8Array;
    decodeOutput(outputBits: Uint8Array): Record<string, unknown>;
    eval(aliceInput: Record<string, unknown>, bobInput: Record<string, unknown>): Record<string, unknown>;
}
//# sourceMappingURL=EmpCircuit.d.ts.map